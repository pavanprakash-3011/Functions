#include <stdio.h>
void sumofdigits(int b){
    int sum=0,c;
    while(b>0){
        c=b%10;
        sum=sum+c;
        b=b/10;
    }
    printf("%d",sum);
}
int main() {
    int a;
    scanf("%d",&a);
    sumofdigits(a);
    return 0;
}
